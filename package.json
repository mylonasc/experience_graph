{
  "name": "experience-navigator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node src/generator.js",
    "serve": "python3 -m http.server 8080 --directory dist",
    "test:visual": "playwright test",
    "test:visual:update": "playwright test --update-snapshots"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2"
  },
  "dependencies": {
    "d3": "^7.9.0"
  }
}
